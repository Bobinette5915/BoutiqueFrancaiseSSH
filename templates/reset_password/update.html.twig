{% extends 'base.html.twig' %}

{% block title %}Mot de Passe oublié - La Boutique Francaise
{% endblock %}

{% block content %}
	<div class="block-small">
		<h1 class="h3 mb-3 font-weight-normal">Réinitialiser mon Mot de Passe
		</h1>
		{% for message in app.flashes('notice') %}
			<div class="alert alert-info">
				{{message}}
			</div>
		{% endfor %}
		{% for message in app.flashes('notice') %}
			<div class="alert alert-info">
				{{message}}
			</div>
		{% endfor %}
		{{ form(form, {'method': 'POST'}) }}
		</div>
	{% endblock %}
