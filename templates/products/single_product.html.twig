<div class="product-item text-center">
                            <a href="{{ path('app_product', {'slug' : product.slug})}}"><img src="/upload/images/{{product.illustration}}" alt="{{product.name}}" class="img-fluid" ></a>
                            <h5 id="test">{{product.name}}</h5>
                            <span id="test1" class="product-subtitle">{{product.subtitle}}</span>
                            <span id="test2" class="product-price">{{(product.price / 100)|number_format(2, ",", ".")}} €</span>
                            
                        </div>